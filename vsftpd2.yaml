---
- name: install, start and enable vsftpd
  hosts: ansible1
  tasks:
          - name: remove vsftpd
            ansible.builtin.package:
                    name: vsftpd
                    state: absent
