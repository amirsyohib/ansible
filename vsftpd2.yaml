---
- name: install, start and enable vsftpd
  hosts: localhost
  tasks:
          - name: remove vsftpd
            ansible.builtin.package:
                    name: vsftpd
                    state: absent
